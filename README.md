# haokah-final-project

## Prerequisites
- you need to have NodeJS installed

## Run project
- run `npm i` to install node modules
- run `npm run dev` to start the development server
- go to http://localhost:3000
